# Deterministic qualification/test dependencies. Runtime dependencies remain in
# requirements.txt; duplicate pins here are only dependencies of the dev tools.
pytest==9.0.2
iniconfig==2.3.0
packaging==25.0
pluggy==1.6.0
Pygments==2.20.0
PyYAML==6.0.3
