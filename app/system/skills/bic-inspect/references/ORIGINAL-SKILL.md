# INSPECT

Run `python scripts/bic.py inspect --scope "<scope>"`. Read the generated Current-Stage Brief and bounded packet. Populate only the prepared proposal and challenge files. Then run the validation command printed in the brief. Do not generate TARGET Scripture.

Follow `.clinerules/` and the protected rewrite-detail rules.
