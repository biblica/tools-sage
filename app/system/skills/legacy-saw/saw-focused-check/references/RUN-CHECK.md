# Targeted Check execution

Execute the current immutable ACT task only. Do not invoke a legacy SAW controller or create another task from inside the task. Answer the one routed focus question and check type, and return only the allowed semantic result. SAGE constructs the findings document, coverage, and receipt.
