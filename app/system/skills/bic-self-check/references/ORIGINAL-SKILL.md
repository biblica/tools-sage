# REVIEW

Run `python scripts/bic.py review --scope "<scope>"`. Review the current TARGET against SOURCE without regenerating it. Use the isolated SELF-CHECK packet and commit only a validated corrected TARGET.

Follow `.clinerules/` and the protected rewrite-detail rules.
