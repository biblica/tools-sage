# RTC reference comparison rules

- Review every WIP and Reference cross-reference span (`\x ... \x*`) under the sealed `x` context policy. Always verify balanced containers and valid field structure. In `NORMAL` mode also compare presence, payload, ordering, and Scripture targets, reporting missing, malformed, unexpected, or materially mismatched cross-references at the owning WIP coordinate.
