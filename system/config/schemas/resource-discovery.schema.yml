schema_id: sage-resource-discovery-1.0
required: [schema_version, scanned_utc, groups, changes, change_count, status]
controls:
  schema_version: '1.0'
  owner: derived_machine_state
  path: SAGEdata/.system/state/resource-discovery.json
  purpose: detect_added_removed_resources_without_opening_resource_content
  scan_scope: immediate_entries_only
  content_reads: false
  groups: [paratext_projects, base_vrs, ol_grk, ol_heb]
  statuses: [BASELINE, UNCHANGED, CHANGED]
