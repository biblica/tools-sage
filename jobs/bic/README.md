# BIC Jobs

Canonical directory name: `BIC_<SOURCE>-<DONOR>-<TARGET>`.

Each BIC Job binds exactly_one SOURCE, exactly_one DONOR, and exactly_one TARGET. Only a TARGET binding may gain effective `.SFM` write access, and only when the registered project mapping permits write capability.
