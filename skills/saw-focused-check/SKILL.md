---
name: saw-focused-check
description: Answer one bounded SAW focus question for one LWC work-in-progress translation.
---
# SAW focused check

Execute only the sealed SAGE governed task described by `task-manifest.json` and `ACT.md`. Use only the routed immutable inputs; do not depend on provider workspace browsing, external file tools, or unlisted context. Write only the outputs declared by the task.

Answer exactly the one bounded focus question and declared check type in `ACT.md` for one LWC WIP translation. Read every listed Skill reference and only the hashed task inputs. If `semantic-saw-signals.json` or `semantic-*.json` is routed, use it only for local-first interrogation/triage; no SEMDOM/index signal is a finding until the bounded translation evidence verifies it. Use the authorised REFERENCE and routed local evidence only. Focused Check never receives OL Scripture. If the bounded question requires original-language adjudication, report that limitation and use the separate governed SAW OL Review operation rather than answering from memory. Do not broaden into general QA, commentary, or rewriting.

Read and reconcile the deterministic preflight and any structural candidates before answering. Apply routed project-grammar rules where relevant and cite their IDs.

Write only `output/findings.json` in `SAW_FINDINGS_2.0` grammar. Copy the exact task ID, scope, operation, stage, focus, and check type. Include a direct bounded `answer`; add findings only when actionable. Cover every expected coordinate and every listed structural candidate. SAW must not edit Scripture.
