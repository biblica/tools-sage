# Focused check execution

Execute the current immutable ACT task only. Do not invoke a legacy SAW controller or create another task from inside the task. Answer the one routed focus question and check type, write only the allowed findings JSON, include the required review receipt, and submit with the exact command printed in `ACT.md`.
